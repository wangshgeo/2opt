#include "SwapPool.h"


int main(int argc, char* argv[])
{
    (SwapPool<1>()).printCount();
    (SwapPool<2>()).printCount();
    (SwapPool<3>()).printCount();
    (SwapPool<4>()).printCount();
    (SwapPool<5>()).printCount();
    (SwapPool<6>()).printCount();
    (SwapPool<7>()).printCount();
    return 0;
}



